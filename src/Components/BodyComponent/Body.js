import React from 'react';
import Searchbar from './SearchBarComponent/Searchbar';

const Body = () => {
  return (
    <React.Fragment>
      <Searchbar />
    </React.Fragment>
  );
};

export default Body;
